{
  "name": "seqsieve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python -m http.server 8000",
    "test": "node -e \"import('./validationTests.js').then(({ runValidationTests }) => { const results = runValidationTests(); console.table(results); if (results.some((r) => !r.pass)) process.exit(1); })\""
  }
}
